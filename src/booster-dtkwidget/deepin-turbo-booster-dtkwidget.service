[Unit]
Description=Qt5 widget application launch booster
After=dbus.socket
Requires=dbus.socket

[Service]
Type=notify
ExecStart=/usr/lib/deepin-turbo/booster-qtwidget --systemd
Restart=always
RestartSec=1
OOMScoreAdjust=-250

[Install]
WantedBy=user-session.target
